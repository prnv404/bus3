const mapping = {
	properties: {
		BusNo: { type: "text" },

		type: { type: "text" },

		depotCode: { type: "text" },

		seats: { type: "integer" },

		Operator: { type: "text" }
	}
};

export { mapping as BusMapping };
